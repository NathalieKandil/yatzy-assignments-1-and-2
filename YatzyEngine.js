// YatzyEngine.js - handles game scoring rules
class YatzyEngine {
    calculateScore(category, diceValues) {
        console.log(`Calculating score for category: ${category}`);
        return 0; // Placeholder return
    }

    isValidSelection(category, diceValues) {
        console.log(`Checking validity for category: ${category}`);
        return true; // Placeholder return
    }
}
export default YatzyEngine;